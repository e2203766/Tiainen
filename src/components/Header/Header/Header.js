import React from 'react';
import NavBar from '../NavBar/NavBar';

const Header = () => {
    return (
        <>
            <NavBar>TIAINEN</NavBar>
        </>
    );
};

export default Header;